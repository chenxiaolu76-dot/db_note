# db-notes

数据库课程笔记站，使用 MkDocs Material 构建。

## 本地预览

```bash
pip install -r requirements.txt
mkdocs serve
```

## 构建

```bash
mkdocs build --strict
```

## 在线部署

推送到 `main` 分支后，GitHub Actions 会自动构建并发布到 GitHub Pages。

> Deployment trigger updated.
